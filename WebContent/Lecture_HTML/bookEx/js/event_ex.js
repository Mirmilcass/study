/*var controller = {
		bombs : 0,

	processBomb: function(bomb) {
		// parseBomb()에다가 인자값으로 bomb를 넣음.
		var location = parseBomb(bomb);
		
		if (location) {
			this.bomb++;
			var hit = model.fire(location);
			
		}
	}
};*/

